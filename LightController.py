import pygame as pg

class LightController: 
    btns = []
    currentLight = 0

    def __init__(self):
        
        self.function = command

    def add(): 
        btns.append(Button(rect=(left,top,50,25), command=()))
